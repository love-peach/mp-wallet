
<app-layout
  headerTitle=""
  headerArrow="{{ false }}"
  isShowFooter="{{ false }}"
>
  <web-view wx:if="{{ webUrl }}" class="app-web-view" src="{{webUrl}}"></web-view>
  <view wx:else>
    {{ webUrl }}
  </view>
</app-layout>

